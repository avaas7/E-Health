<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appointmentapplication">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/dotorjpg"
        android:label="E health build"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppointmentApplication">
        <activity android:name=".Map.MapsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBOWYO2giuSNi55EHapqCpw9kO-5hISkd4" />

        <activity android:name=".Newsfeed.WebView" />
        <activity android:name=".Service.ServiceActivity" />
        <activity android:name=".Doctor.DoctorListActivity" />
        <activity android:name=".Authentication.DoctorSignUpActivity" />
        <activity android:name=".Doctor.PrescriptionListActivity" />
        <activity android:name=".Appointment.PrescriptionActivity" />
        <activity android:name=".Doctor.CheckUpActivity" />
        <activity android:name=".Doctor.PatientListActivity" />
        <activity android:name=".Doctor.DoctorActivity" />
        <activity android:name=".Appointment.AppointmentViewActivity" />
        <activity android:name=".Appointment.AppointmentActivity" />
        <activity android:name=".Authentication.SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Authentication.DoctorSignInActivity" />
            <activity android:name=".SplashScreenActivity"/>
        <activity android:name=".Authentication.SignInActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>